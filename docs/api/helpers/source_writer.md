# `source_writer`

`source_writer` was part of an earlier helper layout and no longer exists as a public module in the current codebase.

Use the `opensampl.create` helpers instead:

- `opensampl.create.create_vendor`
- `opensampl.create.insert_markers`
